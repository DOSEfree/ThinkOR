# 变更日志

本项目采用人工维护的变更日志。

记录原则：

- 只记录已经进入主开发线的重要变化
- 以版本为单位归档
- `未发布` 仅记录已确认会进入下一版的事实性改动

## [0.8.0] - 未发布

### 新增

- 有界澄清判定协议升级：判定标准从“信息是否完美”改为“缺失信息是否致命”，默认直接分析；仅当缺失信息会导致方向性错误时才追问，并新增 `clarification_rationale`（判定理由）随结果返回并展示。
- 想法意图标签：输入可声明使用场景（只是想聊聊 / 个人自用 / 想做成产品 / 已决定要做），`personal` 不再默认假设商业化，`market` 与 `feasibility` 维度按意图调整侧重。
- 澄清判定回归测试集：新增 `evals/clarification_cases.json` 与 `evals/run_eval.py`，配合 CI 契约测试，使每次提示词改动可验证、可对比。
- 版本对比视图：支持在同一想法线内将当前版本与上一版完整方案左右对比（文本修改高亮、列表条目增删标记），历史详情中保留只读对比入口。

### 调整

- Fake LLM 演示输出同步支持意图感知与判定理由，便于本地体验与测试。

## [0.7.5-fix.1] - 2026-08-01

### 修复

- 补足真实飞书归档的 Bot 权限排障引导：身份已就绪但首次归档失败时，页面提供最小文档创建测试命令，并提示根据 CLI 返回的 `console_url` 到开发者后台补充所需权限。
- 将“再次尝试飞书归档”改为 ThinkOR 主题的高对比操作按钮，保留禁用、悬停和键盘焦点状态。

### 调整

- 首次由运行设置创建 `.env` 时，模板默认保留 `IDEAOS_LLM_TIMEOUT_SECONDS=90`，并将 `IDEAOS_FEISHU_ARCHIVE_AS` 与无 `.env` 的回退默认值设为 `bot`；不影响已有 `.env` 或显式系统环境变量。

## [0.7.5] - 2026-07-31

### 修复

- 飞书 CLI 首次应用配置改为由用户在本机终端执行 `lark-cli config init --new`；页面仅提供命令与状态重新检测，不再代理配置子进程或轮询配置二维码。
- 运行设置弹窗的滚动条改为 ThinkOR 深色主题样式。
- 修复 GitHub Actions 的 Linux Mypy 检查无法识别跨平台 `.env` 文件锁实现的问题。

## [0.7.0] - 2026-07-28

### 新增

- 新增本机运行设置：可分别选择 Fake / Real LLM 与 Fake / Real 飞书归档；缺少 `.env` 时按双 Fake 模板创建，且页面只更新非 Secret 的模式开关。
- 新增真实能力状态与首次飞书 CLI 引导：页面可显示脱敏的配置缺失项、CLI/身份状态，并协助完成 CLI 应用配置、二维码 user 授权和重新检测。

### 修复

- Fake Archive 现明确标记为模拟归档，不再生成虚拟飞书链接；旧的固定占位链接会在启动时清理，页面仅打开受信任的 HTTPS 飞书/Lark 链接。
- 修复运行设置在 `.env` 已保存后被能力探测异常误报为失败的问题，并覆盖 `pip install .` 的本机 `.env` 定位、已有 `.env` 切换模式和安全错误响应。
- 修复飞书 CLI 对已验证 `needs_refresh` user 身份的误判，且未完成 CLI 应用配置时不再直接发起授权。

### 安全

- 本地配置写入与飞书授权仅允许 loopback 同源页面携带 CSRF Token 调用；API Key、Token、App Secret、device code 和原始 CLI 输出不被 ThinkOR 返回或持久化。

## [0.6.0] - 2026-07-27

### 新增

- 新增 ThinkOR 横版 Logo、深色主题视觉 token 与面向 loading、错误、删除和归档失败的站内反馈组件。
- 新增失败会话的飞书归档重试，以及生成/完善/合成失败后的安全重试入口；重试不会重新调用 LLM 或创建新版本。

### 调整

- 将公开展示名与页面文案对齐为 ThinkOR，保留 `ideaos_agent` 包名、`IDEAOS_*` 配置前缀、SQLite、API 和飞书归档契约以兼容现有使用方式。
- 将 `/app` 升级为以 `#D7FF00` 为主题色的深色工作台，覆盖空状态、历史详情、分析、follow-up、归档与错误反馈；首页使用局部透视网格，详情页聚焦方案内容。
- 收敛历史链路与归档信息：版本节点可直接跳转并标示 `from V0n`，归档卡合并本地/飞书状态；侧栏支持深色滚动区、展开版本、主题化 Tooltip 和自然的底部渐隐。
- 收紧方案生成与继续完善流程：活动态隐藏重复输入入口，follow-up 自动定位，成功结果回到顶部并显示归档反馈；加载采用低资源三列滚动动画。

### 修复

- 修复图标与文字区域点击未被事件委托识别的问题，并保留历史/方案操作的禁用与 loading 边界。

## [0.5.0] - 2026-07-14

### 新增

- 新增 `SETUP.md`、`LICENSE` 与 `SECURITY.md`，补齐真实能力接入、Apache-2.0 许可和漏洞提交流程。
- 新增 `README_image/` 演示截图资源，用于公开首页展示。

### 调整

- 将 README 与启动说明收敛为面向 GitHub 访客的公开 demo 路径，补齐定位、模式、项目结构和首跑流程。
- 将 `.env.example` 设为默认双 Fake 的零 Secret 首跑模板，真实 LLM 与飞书配置改为按需启用。
- 将包版本更新为 `0.5.0`。

### 移除

- 公开仓库停止跟踪内部协作与诊断资料；本地资料继续在开发环境使用。

## [0.4.5] - 2026-07-10

### 新增

- 新增仅限本地正式历史的搜索接口与侧栏搜索，不搜索 follow-up draft 或外部来源。
- 新增非 ROOT 正式叶子版本删除，级联清理关联 draft、best-effort 删除飞书归档，并返回父节点回退信息。

### 调整

- 为正式历史持久化稳定的全局版本号和父版本标记，并在响应中公开叶子删除可用状态与阻塞原因。
- 收敛侧栏按钮、摘要、版本元信息、展开状态和滚动布局，提高长标题及多版本场景的可读性。

### 修复

- 对齐包版本元数据，移除遗留 `httpx2` 开发依赖，并增加元数据回归检查。

## [0.4.0] - 2026-07-09

### 新增

- 新增整条 idea thread 删除，并以 best-effort 方式清理关联飞书归档。
- 新增用户主动触发的远端归档同步；飞书文档不存在或进入回收站后清理对应本地历史。
- 新增可恢复的 follow-up draft 元数据与入口。
- 将 `/app` 升级为包含分组历史、thread context、业务状态和响应式布局的三段式工作台。

### 调整

- 将 refinement 作为 7 天本地 draft 保存；正式历史只包含 analysis 与 composed plan，消费 draft 后创建新版本并删除快照。
- 历史改为 idea thread/版本展开视图；侧栏独立滚动，移动端保持自然滚动，失败时保留已生成结果。

### 修复

- 修复旧数据缺少 `root_session_id` 时的 thread 展开、详情和删除兼容问题。
- 将测试 SQLite 与开发者真实数据隔离，避免测试污染本地历史。

## [0.3.0] - 2026-07-08

### 新增

- 增加 `root_session_id`，将 analysis、refinement 和 composed plan 聚合为 idea thread。
- 新增 session/thread 历史查询和 `/app` 历史导航，支持从允许的历史节点继续完善。
- 飞书归档增加根会话、父节点和当前节点的 thread context。

### 调整

- 历史只通过用户显式导航参与后续操作，不自动注入 prompt；follow-up 保持有界完善和确认后合成。

### 修复

- 修复 fake LLM 将 follow-up 误判为根分析的问题。
- 修复 Windows 下飞书归档中文渲染和 thread metadata 传递问题。

## [0.2.5] - 2026-07-06

### 新增

- 新增有界 follow-up 闭环：先生成局部完善结果，再由用户确认合成完整新版本。
- 新增结构化 snapshot、父子关系和三类 session 归档，为沿已有结果继续完善提供本地状态。
- `/app` 新增继续完善、确认合成、分类型结果和对应加载状态。

### 调整

- 根分析保存结构化快照，fake LLM 与解析逻辑支持完整 follow-up 演示。

### 修复

- 兼容真实 LLM 返回单对象或 `null` 数组字段。
- follow-up 失败时保留已有结果，复用输入面板并阻止重复提交，同时统一标题与标签。

## [0.2.0] - 2026-07-06

### 新增

- 新增会话归档模型、应用编排和 SQLite 索引，记录 session、状态和归档地址。
- 接入 `lark-cli` 飞书归档、XML 渲染和 fake archiver，并在 `/app` 展示归档状态与文档入口。

### 调整

- 完成态先写 SQLite，再尝试飞书归档并回写结果；归档失败不阻塞主分析结果。
- 增加归档数据库、Fake Archive、飞书身份/目录/超时和标题配置，并保持归档元数据不进入 prompt。

### 修复

- 修复 Windows 下 `lark-cli` 可执行文件解析和飞书归档中文编码问题。

## [0.1.0] - 2026-07-05

### 新增

- 建立从输入校验、提示词、LLM 到九模块结构化输出的单次想法分析闭环。
- 建立 `api / application / domain / infrastructure / prompts` 分层后端，以及支持输入、最多一次澄清和结果展示的 `/app`。
- 支持真实 LLM 与 fake client，默认仅在信息不足时澄清。

### 调整

- 统一 `conda + Python 3.13 + pip`、`ideaos-agent` 环境和 `input_echo` 契约。

### 修复

- 修复 TestClient/httpx 兼容警告，统一 LF 和本地工程产物忽略规则。
